# Creando una tupla usando tuple()
tupla = tuple(["dato1", "dato2"])
print(tupla)

# Otra forma de crear una tupla sin parentesis

tupla = "dato1", "dato2"
tupla = "dato1",  # Si la tupla a crear tiene un solo dato se le agrega "," al final
